<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:converters="clr-namespace:StackOverflowNotifier.Converters;assembly=StackOverflowNotifier.Converters"
	x:Class="StackOverflowNotifier.MainPage"
	Title="Stack Overflow Notifier">
	
	<ContentPage.Resources>
        <ResourceDictionary>
            <converters:DateFormatConverter x:Key="DateFormatConverter"></converters:DateFormatConverter>
        </ResourceDictionary>
    </ContentPage.Resources>
	
	<ContentPage.Content>
		<StackLayout>

			<!-- Header -->
			<StackLayout Padding="10" HeightRequest="100" BackgroundColor="#F37E22">
				<Label Grid.Row="0" Text="Stack Overflow Connector" />
				<Button Grid.Row="1" Text="Tags..." HorizontalOptions="Start"/>
			</StackLayout>

			<!-- Questions -->
			<ListView ItemsSource="{Binding Questions}" RefreshCommand="{Binding RefreshCommand}" IsPullToRefreshEnabled="true" ItemSelected="Question_Selected">
				<ListView.ItemTemplate>
					<DataTemplate>
						<ViewCell>
							<StackLayout>
								<Label Text="{Binding Title}" />
								<Label Text="{Binding CreationDate, Converter={StaticResource DateFormatConverter}}" />
							</StackLayout>
                        	
                    	</ViewCell>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>