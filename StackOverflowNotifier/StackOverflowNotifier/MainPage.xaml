<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="StackOverflowNotifier.MainPage" Title="Stack Overflow Notifier">
	<ContentPage.Content>
		<StackLayout>

			<!-- Header -->
			<StackLayout Padding="10" HeightRequest="100" BackgroundColor="#F37E22">
				<Label Grid.Row="0" Text="Stack Overflow Connector" />
				<Button Grid.Row="1" Text="Tags..." HorizontalOptions="Start"/>
			</StackLayout>

			<!-- Questions -->
			<ListView ItemsSource="{Binding Questions}" RefreshCommand="{Binding RefreshCommand}" IsPullToRefreshEnabled="true" ItemSelected="Question_Selected">
				<ListView.ItemTemplate>
					<DataTemplate>
						<ViewCell>
                        	<Label Text="{Binding Title}" />
                    	</ViewCell>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>